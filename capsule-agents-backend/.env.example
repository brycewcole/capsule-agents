# Required for OpenAI API
OPENAI_API_KEY=your_openai_api_key_here

# Optional: specify model (defaults to gpt-4o)
OPENAI_API_MODEL=gpt-4o

# Optional: enable tools
FILE_ACCESS_TOOL_ENABLED=false
BRAVE_SEARCH_TOOL_ENABLED=false
MEMORY_TOOL_ENABLED=false
A2A_TOOL_ENABLED=false

# Optional: Brave Search API key (if BRAVE_SEARCH_TOOL_ENABLED=true)
BRAVE_API_KEY=your_brave_api_key_here

# Optional: path to SQLite sessions DB
# If not set, defaults to ./data/sessions.db
# For local dev with example data, you can point to:
#   ../examples/development-setup/dev-data/sessions.db
# Or absolute path to your sessions.db
# DB_PATH=../examples/development-setup/dev-data/sessions.db
